# 自动混音说明

自动混音是一个实验性模式。

它会尝试在系统可用的音频设备之间建立一个“输出到输入”的混音链路，以便让语音软件接收到播放出来的音频。

## 限制
- 这取决于 Windows 的系统音频能力。
- 某些系统或驱动不会暴露出可用的混音回传通道。
- 即使启用了自动混音，也可能需要在语音软件中手动选择对应的输入设备。

## 推荐使用方式
1. 勾选“Soundpad 模式”。
2. 勾选“自动混音（实验）”。
3. 在语音软件中选择系统可识别的输入源。